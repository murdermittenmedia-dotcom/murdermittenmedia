import { useEffect, useMemo } from "react";
import { Flame, Medal, TrendingUp, Users } from "lucide-react";
import { trpc } from "@/lib/trpc";

const LOGO = "/manus-storage/mmm_logo_8689da6b.png";
type Entry = { artistName: string; score?: number; xp?: number; totalVotes?: number; fireCount?: number; city?: string | null };

export default function BroadcastRankings() {
  const { data: entries = [], isLoading } = trpc.leaderboard.combined.useQuery({ timeframe: "weekly" });
  useEffect(() => { document.title = "Live Rankings | Murder Mitten Media"; return () => { document.title = "Murder Mitten Media"; }; }, []);
  const ranked = useMemo(() => (entries as Entry[]).slice(0, 8), [entries]);
  return <main className="min-h-screen overflow-hidden bg-[#080808] text-white"><div className="relative mx-auto flex min-h-screen w-full max-w-[1600px] flex-col px-6 py-6 sm:px-10 sm:py-8 lg:px-16 lg:py-10"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_68%_28%,rgba(209,0,0,0.18),transparent_34%),linear-gradient(135deg,#080808,#110708,#080808)]" /><div className="pointer-events-none absolute inset-6 border border-white/[0.07] sm:inset-10 lg:inset-14" /><header className="relative z-10 flex items-center justify-between border-b border-white/10 pb-4 sm:pb-6"><div className="flex items-center gap-3 sm:gap-4"><img src={LOGO} alt="Murder Mitten Media" className="h-10 w-10 rounded-full border border-red-600/60 object-cover sm:h-14 sm:w-14" /><div><p className="font-['Anton'] text-xl uppercase leading-none sm:text-3xl">Murder Mitten <span className="text-red-600">Media</span></p><p className="mt-1 text-[9px] font-bold uppercase tracking-[0.28em] text-white/45 sm:text-[11px]">Live Rankings Broadcast</p></div></div><div className="flex items-center gap-2 rounded-full border border-orange-500/40 bg-orange-500/10 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.2em] text-orange-300 sm:px-4 sm:py-2 sm:text-xs"><span className="h-2 w-2 animate-pulse rounded-full bg-orange-500" /> Trending Now</div></header><div className="relative z-10 grid flex-1 items-center gap-8 py-8 lg:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)] lg:gap-16 lg:py-12"><section className="border-l-4 border-orange-500 pl-6 sm:pl-10"><p className="mb-4 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.38em] text-orange-300 sm:text-sm"><TrendingUp className="h-4 w-4" /> Weekly heat</p><h1 className="font-['Anton'] text-[clamp(4rem,10vw,9.5rem)] uppercase leading-[0.84]">Who’s<br /><span className="text-red-600">moving</span></h1><p className="mt-6 max-w-sm text-base text-white/50 sm:text-xl">The artists making noise across the Mitten right now.</p><div className="mt-10 flex gap-5 text-xs font-bold uppercase tracking-[0.2em] text-white/45"><span className="flex items-center gap-2"><Flame className="h-5 w-5 text-orange-500" /> Live heat</span><span className="flex items-center gap-2"><Users className="h-5 w-5" /> Community</span></div></section><section className="space-y-3"><div className="flex items-center justify-between border-b border-white/10 pb-3"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.24em] text-white/60"><Medal className="h-4 w-4 text-orange-400" /> Top artists</div><span className="text-[10px] uppercase tracking-widest text-white/30">Weekly board</span></div>{isLoading ? <div className="space-y-3">{[1,2,3].map((i) => <div key={i} className="h-16 animate-pulse bg-white/10" />)}</div> : ranked.length === 0 ? <div className="border border-dashed border-white/15 p-6 text-sm text-white/35">Rankings will populate after battles and reviews.</div> : ranked.map((entry, index) => { const value = entry.score ?? entry.xp ?? entry.totalVotes ?? 0; const heat = Math.min(100, Math.max(12, value / Math.max(1, (ranked[0]?.score ?? ranked[0]?.xp ?? value)) * 100)); return <div key={`${entry.artistName}-${index}`} className="border border-white/10 bg-white/[0.035] p-4 sm:p-5"><div className="flex items-center gap-4"><span className={`font-['Anton'] text-3xl ${index < 3 ? "text-orange-400" : "text-white/35"}`}>{String(index + 1).padStart(2, "0")}</span><div className="min-w-0 flex-1"><div className="flex items-baseline justify-between gap-3"><p className="truncate font-['Anton'] text-xl uppercase sm:text-2xl">{entry.artistName}</p><span className="shrink-0 text-xs font-bold text-white/45">{Math.round(value).toLocaleString()}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-red-600 to-orange-400" style={{ width: `${heat}%` }} /></div></div></div></div>; })}</section></div><footer className="relative z-10 flex items-center justify-between border-t border-white/10 pt-4 text-[9px] font-bold uppercase tracking-[0.24em] text-white/30 sm:pt-6 sm:text-[11px]"><span>Detroit · Michigan</span><span className="hidden sm:inline">Where the Industry Watches the Trenches</span><span>Broadcast safe zone</span></footer></div></main>;
}
